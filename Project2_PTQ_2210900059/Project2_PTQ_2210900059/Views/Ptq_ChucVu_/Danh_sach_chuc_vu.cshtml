@model IEnumerable<Project2_PTQ_2210900059.Models.CHUCVU>

@{
    ViewBag.Title = "Danh_sach_chuc_vu";
}

<h2>Danh sách chức vụ</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_CV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Luong_CB)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_CV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Luong_CB)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Ma_CV }) |
            @Html.ActionLink("Details", "Details", new { id=item.Ma_CV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Ma_CV })
        </td>
    </tr>
}

</table>
