@model IEnumerable<Project2_PTQ_2210900059.Models.VAITRO_QUYEN>

@{
    ViewBag.Title = "Vaitro_Quyen";
}

<h2>Vai trò và quyền hạn của người dùng</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ten_VT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ma_Quyen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ten_VT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ma_Quyen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Ma_VT }) |
            @Html.ActionLink("Details", "Details", new { id=item.Ma_VT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Ma_VT })
        </td>
    </tr>
}

</table>
